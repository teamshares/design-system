{
  "name": "@teamshares/design-system",
  "version": "0.0.1",
  "private": true,
  "files": [
    "index.scss",
    "scss-includes/*.scss",
    "utils/*.js"
  ],
  "scripts": {
    "lint": "stylelint --fix --color --config stylelint.config.js scss/",
    "precommit": "lint-staged --quiet"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0",
    "stylelint": "^15.0.0",
    "stylelint-config-sass-guidelines": "^10.0.0"
  }
}
